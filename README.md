# ParallelX
ParallelX is a production grade parallel workflow engine in Python. Define DAG workflows in JSON, then run tasks concurrently using ProcessPool (CPU) or ThreadPool (I/O). Includes retries with exponential backoff, optional deterministic disk caching, structured logging, run summaries, CLI, tests, Docker, and examples. Built for clean packaging, CI.
